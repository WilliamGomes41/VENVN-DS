# Changelog

All notable technical changes to V&VN Data Services are recorded here.

## [Unreleased]

### Added
- Protocol v2.30 Phase 1 on the existing kernel/console (one code PR). Required candidate fields, richtlijn type contracts (Aanbeveling / Definitie / Voorwaarde / Uitzondering / Feitelijke constatering → `explanation` / Toelichting), hard admission gate, deterministic `reason_codes`, dJG and the named regressions, minimal adjacent `context_before`/`context_after`. Ordinary review queue is `gate_result=allowed` only; blocked candidates stay inspectable in a blocked/audit lane. Phase 1 MUST NOT emit `context_scan_not_done` for every candidate and MUST NOT empty the ordinary queue with that code. Boom `path`/`node`/`outcome` stay Protocol v2.25 (no richtlijn `type_contract_incomplete`). Passage register, deep context scan, and review UI rewrite are later phases. `publish()` remains G2-BLOCKED. Not Azure ZIP, not nurse UI, not numpy/sklearn, not `HANDOFF.md`.
- Protocol v2.30.0: owner lock 2026-09-05 (William; Metis CoS GO) — one focused protocol cut with two independently testable acceptance blocks. Block A — hard admission gate / object contract / reason codes: a knowledge object MAY be proposed ONLY when all required fields for the proposed type are filled with literal localizable source text; missing required field → `gate_result=blocked`; soft scores (`relevant`/`complete`/`understandable`) MAY rank only and MUST NOT open the hard gate; There MUST be NO tradeoff — UI polish MUST NOT excuse bad candidates; current knowledge-object quality is ~5/10 (fail, not a shippable bar); soft scores / volume / “ship then fix” MUST NOT open the hard gate; blocked candidates MUST NOT enter the ordinary review queue; the model MUST NOT fill gaps with “impliciet” prose; admission MUST require full carrying sentence AND `subject_span` AND `predicate_span` AND valid locator AND complete type_contract AND type_evidence AND no unresolved core reference AND no incomplete comparison AND context scan done AND found constraints processed; required candidate fields before human review include `candidate_id`, `document_id`, `document_version`, `source_hash`, `section_path`, `source_locator_start`/`end`, `source_text_exact`, `candidate_text`, `subject_span`, `predicate_span`, `proposed_type`, `type_evidence_spans`, `context_before`/`after`, `conditions_detected`, `exceptions_detected`, `comparison_markers`/`targets`, `references_detected`/`resolved`, `abbreviations_detected`/`resolved`, `related_candidates`, `gate_result` (`allowed`|`blocked`), `reason_codes[]`; hard blocks MUST include at least `incomplete_sentence`, `subject_missing`, `predicate_missing`, `unresolved_reference`, `comparison_target_missing`, `abbreviation_unresolved`, `table_of_contents_entry`, `editorial_transition_only`, `recommendation_evidence_missing`, `condition_target_missing`, `exception_target_missing`, `supported_object_missing`, `no_independent_claim`, `duplicates`, `source_fidelity_failure`; type contracts: Aanbeveling (`actor_of_scope` + `recommended_action` + `action_object_or_goal` + `recommendation_evidence_span`), Definitie, Voorwaarde, Uitzondering, Feitelijke constatering, Toelichting (must link supported object; not independently publishable by default); context scan: candidate paragraph + prev/next + current+ancestor headings; atomicity is one independently confirmable meaning including required conditions/exceptions — NOT always one word/one sentence; `source_text_exact` immutable; MUST NOT silently drop passages; no duty to objectify every sentence. Mandatory dJG regression “De dJG wordt in Nederland vaker gebruikt.” MUST NOT enter the ordinary queue as Aanbeveling; `reason_codes` MUST include `recommendation_evidence_missing`, `comparison_target_missing`, `abbreviation_unresolved` (when unresolved). Also regress one-word, unresolved ref, incomplete comparison, false recommendation, full “adviseert” recommendation, lone exception, recommendation+exception. Block B — reviewer passage-flow: documentpositie SUPERSEDES primary Relatie bevestigen / always-visible TOC+full parent lists; primary UI MUST use ordinary language only (**Gevonden onder** / **Dit klopt** / **Andere kop kiezen**); full hierarchy ONLY after **Andere kop**; body headings only; TOC excluded; **Open volledige richtlijn** / broncontext MUST show surrounding page/paragraph context with the exact span marked — MUST NOT merely enlarge the same truncated card sentence; review UI order + ONE save (A–F) with **Review opslaan en volgende**; type UI MUST show Metis proposal + Dit klopt / Type wijzigen — MUST NOT start from only “nog niet bevestigd”; v2.28 Sterkte-on-confirmed-type remains. Metrics (precision, type accuracy, context completeness, coverage vs gold, review burden) MUST be required by protocol; gold standard required before claiming extract quality. This PR is protocol-only. ROADMAP MUST state four Forge phases after separate Metis GOs: (1) fields+contracts+hard gate+reason codes+dJG regression + minimal adjacent context (richtlijn inhoudelijke candidates; boom stays v2.25 until a separate boom-gate GO; MUST NOT empty the ordinary queue with `context_scan_not_done`; passage register is not a Phase-1 admission prerequisite) (2) context/refs/abbrev/comparisons/expand-merge (3) review UI + open-bron real context + collapsed document position (4) passage register + coverage + gold + metrics. Feitelijke constatering confirm → `explanation`; MUST NOT serve as recommendation/handelingsadvies. MUST NOT implement Forge phases in this PR. MUST NOT open G2/`publish()`, Azure ZIP, nurse UI, or recreate HANDOFF. HANDOFF.md MUST NOT be recreated. G2 remains BLOCKED; `publish()` stays G2-BLOCKED. Protocol-only; does not implement console, extract or Azure. v2.14 is not this and is not next. v2.25 boom path UNCHANGED. v2.26 Klasse wijzigen architecture UNCHANGED. v2.27 unpublished-delete Documentenhiërarchie + type-to-confirm UNCHANGED. v2.28 Sterkte gate UNCHANGED. v2.29 temporary production-only deploy UNCHANGED. Four layers UNCHANGED.
- Protocol v2.28 Blocks A+B on the existing kernel/console (one code PR). Block A: mark TOC (inhoudsopgave) separately from body headings; parent-choice / heading navigation primarily from the document body; outline hierarchy `5` → `5.4` → `5.4.1` → `5.4.2`; near-duplicates leave the choice list only; freeze/audit anchors stay; source/extract order fallback; structurally valid parent only — invalid nearby parents MUST NOT bind / MUST NOT be default structure; TOC crumbs MUST NOT bind as parent; TOC inference ends on outline/source evidence (not only exact title repeat); `parent` relation direction is current-as-parent; parent-choice rows are links plus radios on the relation form. Block B: machine-proposed `recommendation` MUST NOT show Sterkte; Sterkte visible+active ONLY on stored/confirmed `recommendation` or actionable boom `outcome`; live UI appear/disappear before submit; type change away clears active strength on `review_object` and `confirm_object_type` (audit history MAY keep the old value); machine-proposed strength stays hidden until type confirm; hidden Sterkte `<select>` is `disabled` until the gate is live. Separate acceptance tests: A pass is not B pass. `publish()` remains G2-BLOCKED. Not Azure ZIP, not nurse UI, not Product API boom serving, not `HANDOFF.md`.
- Protocol v2.27 unpublished-delete Documentenhiërarchie only + type-to-confirm on the existing console (one code PR). Remove Verwijder unpublished document from Inleveren, Review, Publiceren, Accounts and every other room. No separate Delete room. On Documentenhiërarchie only: type-to-confirm the exact document title (title shown; wrong/empty/partial title does not delete). Keep unpublished-only, confirm, audit-ledger row, no published-projection delete, no SSH/wipe of `/home/data`, four-eyes not required. `publish()` remains G2-BLOCKED. Not selective class invalidation, not v2.28 heading/strength, not Product API, not Azure ZIP, not nurse UI, not `HANDOFF.md`.
- Protocol v2.28.0: owner lock 2026-09-05 (William) — one focused protocol cut with two independently testable acceptance blocks. Block A — structural heading / parent-list navigation: current extract order is useful for provenance but unsuitable as researcher navigation; MUST NOT use naive global numeric sort of all headings (TOC + body merge risk); MUST recognize and mark table-of-contents (inhoudsopgave) items separately from body headings; parent-choice / heading navigation list MUST primarily use headings from the document body; MUST derive hierarchy from outline numbers where reliable (`5` → `5.4` → `5.4.1` → `5.4.2`); near-duplicates MAY be removed from the choice list only; all source anchors MUST remain in freeze/audit trail; source/extract order remains fallback; MAY show page number or source locator to distinguish same-named headings; a parent MUST be structurally valid; heading `5.4.1` MUST NOT get heading `2` as parent merely because it was extracted nearby; invalid parent proposals MUST NOT bind / MUST NOT be offered as default structure; parent list shows a deduplicated, hierarchically ordered document structure from the main text. Block B — strength stamp gating (confirmed type only): a machine proposal `recommendation` MUST NOT activate/show Sterkte; Sterkte visible and active ONLY when stored/confirmed type is Aanbeveling (`recommendation`) OR an actionable boom `outcome`; on type change in the browser, Sterkte MUST appear/disappear before submit (live UI); if type changes away, Sterkte disappears immediately and any previously chosen strength MUST NOT be actively saved on that object (old value MAY remain in audit history only); machine MAY still propose a strength value but it MUST stay hidden/inactive until the user confirms the relevant type; SUPERSEDES any reading of Protocol v2.16/v2.17 that stamp UI MAY appear based solely on `proposed_object_type` without human type confirm; v2.16/v2.17 stamp-on-recommendation law remains; the gate becomes confirmed/stored type. This PR is protocol-only. Next code after this protocol's own Metis GO is Forge on the existing console for exactly Blocks A+B with separate acceptance tests. MUST NOT implement v2.27 delete in this protocol PR. Where this delta and Protocol v2.27 conflict on which implementation is next: v2.27 delete wave may already be in flight under separate Metis GO; this delta's next Forge wave is A+B after its own GO. ROADMAP must state both. MUST NOT open G2/`publish()`, Azure ZIP, nurse UI, or recreate HANDOFF. HANDOFF.md MUST NOT be recreated. G2 remains BLOCKED; `publish()` stays G2-BLOCKED. Protocol-only; does not implement console, extract or Azure. v2.14 is not this and is not next. v2.25 boom path UNCHANGED. v2.26 Klasse wijzigen architecture UNCHANGED. v2.27 unpublished-delete Documentenhiërarchie + type-to-confirm UNCHANGED. Four layers UNCHANGED.
- Protocol v2.27.0: owner lock 2026-09-05 (William) — unpublished document delete MUST be available from exactly one console place: **Documentenhiërarchie**. MUST NOT offer Verwijder unpublished document (or equivalent delete control) from Inleveren, Review, Publiceren, Accounts, or any other room. MUST NOT invent a separate Delete room/kamer. Before delete executes the operator MUST type the exact document title into a confirmation field (type-to-confirm). The console MUST show the title clearly so the operator can copy/read it — safety measure against accidental/fast delete, not a puzzle. Without an exact title match, delete MUST NOT run. SUPERSEDES any reading of Protocol v2.20 that delete MUST appear on the document card / Review-chooser as alternative surfaces. Replace with: Documentenhiërarchie only + type-to-confirm exact title. Unchanged from v2.20: only unpublished captured snapshots MAY be deleted; MUST confirm before execute (now includes type-to-confirm title); MUST write an audit-ledger row; MUST NOT delete a published projection; MUST NOT treat SSH/wipe of `/home/data` as the product path; four-eyes NOT required. `PROTOCOL.md` is every-guideline law, not Continentie-only. HANDOFF.md MUST NOT be recreated. G2 remains BLOCKED; `publish()` stays G2-BLOCKED. This PR is protocol-only. Next code is Forge on the existing console for exactly: remove delete controls from every surface except Documentenhiërarchie; add type-to-confirm field requiring exact document title (title shown); keep existing unpublished-only / audit-ledger / no published delete / confirmation; tests (tests-before-code). MUST NOT open G2/`publish()`, Azure ZIP, nurse UI, selective class-change work, or recreate HANDOFF. Klasse wijzigen first wave is already on `main` (PR #97). Protocol-only; does not implement console, extract or Azure. v2.14 is not this and is not next. G2 remains the publication blocker. v2.25 boom path UNCHANGED. v2.26 Klasse wijzigen architecture UNCHANGED. Four layers UNCHANGED.
- Protocol v2.26 Klasse wijzigen first wave on the existing kernel/console (one code PR). Rename researcher action **Promoveren** → **Klasse wijzigen**. Cross-model (any non-boom ↔ `beslisboom`) blocks direct class change and requires re-extract from the same freeze → new object graph → full review; prior objects remain as audit history. Same-model keeps the existing safe full re-review (temporary; selective invalidation is later). Before confirm the console shows consequence (source unchanged; same-model vs cross-model; objects kept vs re-extract; full re-review in this wave). Class change is recorded as audit-event `document_class_changed`. Source freeze bytes / SHA-256 / title / version / provenance do not change. Product API boom serving is not activated. G2 remains BLOCKED; `publish()` remains G2-BLOCKED. Not selective invalidation, not published-candidate fork, not full `previous_review` schema, not Azure ZIP, not a nurse tree player, not `HANDOFF.md`.
- Protocol v2.26.0: owner lock 2026-09-05 (William) — combined architecture + first code wave. **Klasse wijzigen** / controlled reclassification. A document class change MUST invalidate only what that class change substantively affects. MUST NOT be a silent total wipe of all review state as the only story. Current `promote_class` reset SUPERSEDED for the target architecture; temporary safe full re-review allowed in the first implementation wave. Class change MUST NOT alter freeze bytes, SHA-256, title, version, or provenance of the source. Class is how Metis interprets the document, not what the source is. richtlijn-path Klassen (`richtlijn` | `handreiking` | `artikel` | `transcript` | `podcast`) share the same review model / object typeset; beslisboom-path Klasse is `beslisboom` (`path` / `node` / `outcome`; Protocol v2.25 UNCHANGED). Cross-model = any transition where from-Klasse and to-Klasse select different review paths (boom vs non-boom). Transition matrix is normative intent: `transcript` → `artikel` light reclassification / same model; `artikel` → `handreiking`, `handreiking` → `richtlijn`, `richtlijn` → `artikel` substantive re-review (same model); `richtlijn` → `beslisboom` and `beslisboom` → `richtlijn` re-extract + full review (cross-model); any non-boom ↔ `beslisboom` and `beslisboom` → other non-boom are cross-model. Same-model: keep objects, locators, fragment bounds, content hashes, class-independent relations; re-open only class-dependent confirmations; target architecture = selective invalidation. Cross-model MUST NOT direct class change / re-label objects across models; MUST block direct change and REQUIRE re-extract from the same freeze → new object graph → full review; prior object set MUST remain as audit history. MUST NOT conceptually wipe `validated_by` / `validation_date` / `review_snapshot_hash` alone; prefer invalidate with reason (`document_class_changed`) and keep prior review reconstructible. Published never rewritten: MUST NOT mutate a live published release back to unpublished; MUST create a new draft candidate version; published v1 remains until v2 is published through the existing fail-closed publish/G2 path. Rename console action **Promoveren** → **Klasse wijzigen**; before confirm MUST show consequence (source unchanged; same-model vs cross-model; objects kept vs re-extract required; how many reviews reopen when known / that full re-review applies in the first wave). SUPERSEDES any reading that Inleveren needs a separate path control distinct from Klasse remains v2.25 law. This PR is protocol-only. Next code is Forge on the existing console for exactly the Klasse wijzigen first wave after a separate Metis GO (rename; enforce matrix; same-model temporary full re-review; show consequence; audit-event; source/SHA unchanged). MUST NOT implement selective invalidation, published-candidate fork, or full `previous_review` schema in that first code wave unless separately GO’d. ROADMAP marks selective + published-candidate as next after the narrow wave. MUST NOT open G2/`publish()`. Until that Forge GO, no Cloud Shell ZIP required for this delta alone. HANDOFF.md MUST NOT be recreated. G2 remains BLOCKED; publish() remains G2-BLOCKED. Protocol-only; does not implement console, extract or Azure. v2.14 is not this and is not next. G2 remains the publication blocker. v2.25 boom path UNCHANGED. Four layers UNCHANGED.
- Protocol v2.25 beslisboom path on the existing kernel/console (one code PR). Closed Klasse set includes `beslisboom`; Klasse choice selects the review path; no second Inleveren chooser labeled “path”. Klasse=`beslisboom` uses closed boom types `path` / `node` / `outcome` only (scorelist item is `node` + flag). Other Klassen keep the existing richtlijn-style stacks and MUST NOT invent boom types. `outcome` MUST bind via `applies_if`; proposed `applies_if` is not auto-confirmed on approve. Condition-fused-only, multi-bullet and empty/placeholder (`UitkomstX_Y_titel`) fail review. Actionable outcomes take DOEN / OVERWEEG / NIET DOEN and MUST carry a strength; «geen actie» maps to `niet_doen` / explicit no-action. High-risk fields are preserved on boom extract so four-eyes can fire. Boom ingest is a hashed JSON freeze + exact byte-span locators + SHA-256; empty local freezes without nodes+outcomes are rejected; live kennisplatform REST is not the sole source of truth; `story.html` alone is insufficient. Canonical review hash includes `no_action` and `metadata` when present. `beslisboom` is lighter than `richtlijn`; a confirmed richtlijn recommendation of the same family outranks a boom outcome. Product API boom serving is not activated. G2 remains BLOCKED; `publish()` remains G2-BLOCKED. Not Azure ZIP, not a nurse tree player, not `HANDOFF.md`.
- Protocol v2.25.0: owner lock 2026-09-04 (William) after comparing four kennisplatform Storyline booms (Valrisico tree 4, Fractuurpreventie tree 2, Mantelzorg tree 3, Eenzaamheid tree 1): one stable family. MVP **beslisboom** document class with its own object model pad / node / uitkomst (English closed types: `path`, `node`, `outcome`). Owner photo-lock 2026-09-04 on Inleveren **Klasse** UI: closed Klasse set MUST include `beslisboom` alongside `richtlijn` / `handreiking` / `artikel` / `transcript` / `podcast`; Klasse choice selects the review path; SUPERSEDES any reading that Inleveren needs a separate path control distinct from Klasse; Operators MUST NOT invent other Klasse values. Choosing Klasse = `beslisboom` MUST select the boom review path (`path` / `node` / `outcome`). Choosing Klasse = `richtlijn` | `handreiking` | `artikel` | `transcript` | `podcast` MUST select the existing non-boom (richtlijn-style) review path / stacks; MUST NOT invent boom types on those classes. Family × class remains; paths MAY differ for the same family. Beslisboom MUST be in the MVP (hangable working method). SUPERSEDES the Protocol v2.7 reading that kennisplatform `story.html` boom players MUST stay entirely out of the MVP / first console as a knowledge class. The Storyline player package is not the Product API surface and MUST NOT be the nurse console. SUPERSEDES any reading that the only MVP document classes are guideline HTML/PDF without a boom path. v2.8 nurse-tree rule stays for console UX; reviewing beslisboom objects as researchers is allowed. Closed boom types only on the beslisboom path; Operators MUST NOT invent others. Scorelist item MAY be a `node` + documented flag; MUST NOT a fourth closed boom type. Richtlijn path keeps v2.12 types and MUST NOT require boom types. `outcome` MUST bind via `applies_if`; MUST NOT silently fuse condition into outcome as the only representation. Confirm `path` as structure (batch OK); `node` slow if it gates advice; `outcome` as advice with DOEN/OVERWEEG/NIET DOEN when actionable; «geen actie nodig» maps to `niet_doen` or explicit no-action that MUST NOT be served as positive advice; empty/placeholder outcomes (`UitkomstX_Y_titel`) MUST NOT pass review; high-risk keeps four-eyes; bronpassage + open-original against boom freeze bytes. Hashed freeze of canonical boom knowledge (node + outcome text at minimum); MUST NOT treat live kennisplatform REST `/wp-json/beslisboom/v1/outcomes` as the sole source of truth; live URL-HTML `story.html` alone remains insufficient; byte-freeze + locators + SHA-256 required. `beslisboom` is a lighter/derived class than `richtlijn`; boom outcomes MUST NOT silently substitute for unpublished/missing guideline recommendations. Next code is Forge on the existing kernel/console for exactly the beslisboom path wave (Klasse includes beslisboom; Klasse choice selects review path). MUST NOT activate Product API boom serving in that first code wave unless separately GO’d. MUST NOT open G2/`publish()`. Until that Forge GO, no Cloud Shell ZIP required for this delta alone. HANDOFF.md MUST NOT be recreated. G2 remains BLOCKED; publish() remains G2-BLOCKED. Protocol-only; does not implement console, extract or Azure. v2.14 is not this and is not next. G2 remains the publication blocker.
- Protocol v2.24 console vs retrieval requirements split on the existing kernel/repo (one code PR). Split the deploy package, not the product idea. Runtime-scheiding mag veranderen; protocol- en publicatiegrenzen niet. Console Azure/pack path (`requirements-console.txt`, `create_azure_deploy_package.sh`, `azure_deploy_package.py`) MUST NOT vendor numpy, sklearn, scipy, or scikit-learn. Console ZIP MAY include FastAPI, gunicorn, uvicorn, python-multipart, PyMuPDF, jsonschema, azure-identity, azure-storage-blob (G2 client remains fail-closed). Oryx output.tar.zst of a fat vendor tree on B1 is refused. `embedding_provider_v1` / vector/hybrid retrieval / `product_api_v1` stay in the repo under the heavier `retrieval` extra (`requirements-retrieval.txt`). console_asgi / operations_console_* MUST NOT import those at process start. Keep `service_app.py` AND `product_api_v1.py`. Import-graph and pack tests fail if the console graph or shared kernel pulls numpy/sklearn/scipy. No live Product API service. No retrieval in the console. Not Cloud Shell, not Azure ZIP, not a new App Service, not PR #82, not `HANDOFF.md`. G2 remains BLOCKED; `publish()` remains G2-BLOCKED; this split does not open publish() or G2.
- Protocol v2.24.0: owner lock 2026-09-03 (William): split the deploy package, not the product idea. Two doors already exist (operations console vs Product API). Tonight B1 died because one requirements.txt vendored numpy/sklearn/scipy into vvn-metis-console. Owner addendum: the temporary deploy split MUST NOT become two systems. Runtime-scheiding mag veranderen; protocol- en publicatiegrenzen niet. PROTOCOL, object formats, freeze rules, and publish logic are one shared kernel. Console MUST NOT interpret review/object/freeze/publish rules differently from Product API. No second law in the API. Shared kernel modules MUST NOT import numpy, sklearn, scipy, or scikit-learn. A console import of shared code MUST NOT pull those in. Package boundaries are law. Tests in this protocol PR MUST fail if console_asgi / operations_console_* import graph includes those packages. Live Review console (vvn-metis-console, console_asgi / operations_console_*) Azure deploy package MUST NOT vendor numpy, sklearn, scipy, or scikit-learn. Console ZIP MAY include FastAPI, gunicorn, uvicorn, python-multipart, PyMuPDF, jsonschema, azure-identity, azure-storage-blob (G2 client remains fail-closed). Oryx output.tar.zst of a fat vendor tree on B1 is refused; console deploy MUST stay small enough that B1 starts without a 136MB tar extract. console_asgi MUST NOT import embedding_provider / vector retrieval / hybrid retrieval at process start. Product API / TF-IDF / LocalCharTfidfEmbeddingProvider stays in the repo with its own requirements/runtime later; MUST NOT go live in this wave. No new App Service in this PR. Product API MUST NOT be deployed in the same App Service worker as the Review console. Shared across doors: PROTOCOL, freeze bytes (SHA-256), and later G2 published objects. MUST NOT share unpublished review store or console login accounts as API entitlement. Console accounts are not API tenants. When Product API goes live the data boundary MUST be technically enforceable (separate access, credentials, and storage rights). Unpublished review store and researcher console accounts MUST NOT be reachable with Product API credentials. “We don’t do that” is not enough. Thin B1 console MUST NOT accrete subscriber/retrieval features as one more small thing. Functional boundary: review work (ingest, tree, Beoordeel, unpublished delete, four-eyes) in the console runtime. Retrieval and subscriber functions outside that runtime. Console classification remains closed taxonomy + context-aware splitter (rules), not sklearn. Keep service_app.py AND product_api_v1.py (two products). Do not silent-delete CLI review-queue. Console remains the researcher duty queue. Next CODE after this protocol (not this PR): split console vs retrieval requirements, prove console_asgi import graph has no sklearn/numpy, one thin console ZIP. Cloud Shell of that ZIP is a later live step, not tonight. MUST NOT implement the requirements split in this PR. Two Cloud Shell ZIPs of different SHAs still refused. A later thin ZIP of the v2.24 implementation SHA is the one live ZIP. PR #82 stays closed/unmerged. Wave B still after a healthy console ZIP + ingest. First DELETE cut remains done/law. HANDOFF.md MUST NOT be recreated. G2 remains BLOCKED; publish() remains G2-BLOCKED; this split does not open publish() or G2. Protocol-only; does not implement console, extract or Azure. v2.14 is not this and is not next. G2 remains the publication blocker.
- Protocol v2.23 first DELETE cut on the existing kernel/repo (one code PR). Deleted nine zero-caller `src/` modules after re-proving no live callers: `extract_pdf.py`, `semantic_transform.py`, `validation_workflow.py`, `build_second_review_queue.py`, `pre_step5_gate.py`, `import_expert_validation.py`, `reconcile_legacy_review.py`, `evaluate_safe_retrieval.py`, `build_retrieval_document.py`. `scripts/run_integrity_sprint.sh` now uses committed fixtures (`data/fixtures/baseline_v0_1/fractuurpreventie_page15_semantic_v21.jsonl` and the matching raw extract) instead of `python -m src.semantic_transform_v21`. Does not merge v2/v21/generic. Does not delete `semantic_transform_v2.py`, `prepublication_gate_v2.py`, `validation_workflow_v2.py`, `apply_second_review.py`, `canonical_store.py`, `service_app.py`, or `product_api_v1.py`. Does not touch console, splitter, wave A `reject_candidate`, or CLI `review-queue`. G2 remains BLOCKED; `publish()` remains G2-BLOCKED. Not Cloud Shell, not ZIP, not Azure, not wave B, not PR #82, not `HANDOFF.md`.
- Protocol v2.23.0: owner asked Auditor for a code-surface review at `main` HEAD `a566af56c8c88e76cb4de7fa51642b408705da02` and handed the result to Metis (PROTOCOL/ROADMAP owner). Auditor verdict REQUEST SIMPLIFICATION. Not GD-03, not publication. G2 remains BLOCKED. Owner lock 2026-09-03 SUPERSEDES the earlier v2.23 claim that Cloud Shell ZIP of `a566af56` happens BEFORE the first DELETE cut. Live path: first DELETE cut, then one ZIP of that SHA. First DELETE cut (nine zero-caller src modules + integrity-sprint fixture retarget) happens on main BEFORE any Cloud Shell ZIP. Then ONE Cloud Shell ZIP of that resulting SHA (A+C+D already on main, plus v2.20 delete control, plus the first DELETE cut). MUST NOT ZIP a566af56 if that forces a second ZIP after cleanup. Two Cloud Shell ZIPs is refused. PR #82 stays closed/unmerged. Wave B still after that one ZIP + ingest. first DELETE cut, zero-caller src/ modules only: extract_pdf.py; semantic_transform.py; validation_workflow.py; build_second_review_queue.py; pre_step5_gate.py; import_expert_validation.py; reconcile_legacy_review.py; evaluate_safe_retrieval.py; build_retrieval_document.py. Do NOT delete semantic_transform_v2.py, prepublication_gate_v2.py, validation_workflow_v2.py, apply_second_review.py (subprocess-locked by tests/test_protocol_v2.py). Do NOT delete canonical_store.py. Keep service_app.py AND product_api_v1.py (inspection vs Product API; two products, not leftovers). Live ingest uses extract_pdf_v2, extract_html_v1, semantic_transform_generic_v1, prepublication_gate_v3. Keep operations_console_v1.py vs operations_console_app.py split; no template engine; wave A splitter + reject_candidate; test_protocol_v2_* and test_v2* both kept. Same or next change: point scripts/run_integrity_sprint.sh at committed fixtures instead of python -m src.semantic_transform_v21; do not merge v2/v21/generic. Dual live path (plan, do not silent-delete): CLI review-queue vs console review_stacks / slow_review_duty; console is the researcher duty queue. Protocol-only; does not implement console, extract or Azure. Next implementation is one deletion PR on main BEFORE any Cloud Shell ZIP, existing pytest only, MUST NOT touch the splitter or console in that PR. Implementation MUST re-prove no live callers before each delete. HANDOFF.md MUST NOT be recreated. No G2 PASS, no Blob grant, no Azure ZIP as this PR, no Cloud Shell, no LLM, no v2.14. v2.14 is not this and is not next. G2 remains the publication blocker.
- Protocol v2.22 waves C and D on the existing kernel/repo (one code PR). Wave C finishes the PR #82 faults: `bash scripts/create_azure_deploy_package.sh` is executable and invoked via bash; the Azure ZIP vendors `.python_packages` from `requirements.txt` (git-archive-only is not enough); per-environment storage account/container via app settings `G2_STORAGE_ACCOUNT` / `G2_BLOB_CONTAINER` / `CONSOLE_DATA_ROOT` (no secrets in Git); separate `metis-deploy-test` vs `metis-deploy-production` identities so test cannot production and production cannot test. `deploy-test` / `deploy-production` stay inactive / fail-closed (`workflow_dispatch` only; no `on.push`) until GitHub variable `METIS_TEST_APP_READY=true` after Azure App Service `vvn-metis-console-test` exists; merge to `main` MUST NOT auto-deploy (owner lock 2026-09-03). This PR does not create that test App Service. MUST NOT merge PR #82. ZIP does not contain runtime data; packaging refuses to write `/home/data`. Wave D inventories `/home/data/metis-console` (accounts/roles, snapshots, review ledger, canonical objects, derived projections); export/backup, controlled restore to a clean environment, integrity check; `--clean true` wipes wwwroot and MUST NOT delete runtime data. No large database migration; documented boundary: a managed database is required before multiple App Service instances or concurrent multi-reviewer writes. G2 remains BLOCKED; `publish()` remains G2-BLOCKED. Not wave B, not Azure ZIP, not ingest, not G2 PASS, not Blob grant, not v2.14, not LLM, not publish PASS.
- Protocol v2.22.0: bounded supersession of Protocol v2.21 §3 order A then B then C then D, for next-implementation order only. Waves themselves unchanged. Historical v2.21 wave definitions remain law. Wave A is already in code on `main` `512ffa5026d06ff804434ddf4d07a08a36c02305`. After A: wave C (finish PR #82 faults; packaging via bash or executable; ZIP MUST be fully deployable with dependencies — git-archive-only is not enough; per-env storage via app settings; no secrets in Git; separate test vs production deploy identities; MUST NOT activate deploy-test/deploy-production until Azure test App Service `vvn-metis-console-test` exists; merge to `main` MUST NOT auto-deploy to a missing test app) AND wave D (`/home/data` inventory, export/restore, proof that `--clean true` wipes wwwroot and MUST NOT delete runtime data). THEN one Cloud Shell / production ZIP of that controlled SHA (A+C+D), not a live-URL ingest. Ingest of a new freeze MUST be after that ZIP, not before (live still runs pre-wave-A extract until ZIP). Wave B (G2 evidence/smoke) AFTER that ZIP. G2 still BLOCKED; `publish()` still G2-BLOCKED; ZIP does not open publication. v2.20 unpublished-delete remains on main, not a fifth wave. Protocol-only; does not implement console, extract or Azure. Next implementation is wave C and wave D, then stop for William Cloud Shell ZIP of that SHA, then ingest. No G2 PASS, no Blob grant. MUST NOT start Azure test app in this protocol PR. MUST NOT implement C/D in this PR. MUST NOT rewrite v2.16–v2.21 files except index/conflict pointers. v2.14 is not this and is not next. G2 remains the publication blocker.
- Protocol v2.21 wave A on the existing kernel/extract: context-aware splitter (`split_context_aware_units`) plus a unit-testable reject function (`reject_candidate`) with explicit exceptions for short real definitions and official headings. An inhoudelijk knowledge object is one complete independently readable meaning unit with bronpassage+locator. Chrome/nav/list numbers/loose labels/empty/too-short are filtered BEFORE object creation. Trailing clauses attach to the previous sentence; DOEN/OVERWEEG/NIET DOEN attach to the following advice sentence and are not objects. Identical `clean_text` from the same freeze is emitted once. Inleiding stays a real heading; Home/Tools/Richtlijnen/Meedenken are chrome. Continentie regression fixtures cover the known fail patterns; those patterns MUST NOT land as standalone inhoudelijk objects in the review duty queue. v2.16 tiny-objects, v2.17 chrome and v2.18 no-duplicate-sentence still hold. Serving/G2 unchanged; `publish()` remains G2-BLOCKED; unclassified is never served. No infrastructure, Azure, G2, PR #82, backup/restore, Cloud Shell, ZIP, v2.14, LLM or publish PASS. Waves B/C/D are not this PR. Freeze store paths remain confined (allowlist filename/snapshot/date/version before any filesystem join). Protocol PR #84 remains separate.
- Protocol v2.21.0: owner paused Cloud Shell and locked a four-wave controlled-use program (A then B then C then D). Priority: source integrity, clear knowledge objects, safe environment isolation, recoverability. Cloud Shell / production ZIP stay off until wave A is on a controlled SHA. Maps, does not rewrite, existing v2.16–v2.20 law. v2.16–v2.18 already forbid tiny objects, stamp-as-heading/object, truncated/trailing clauses, chrome objects, identical `clean_text`. v2.19 is duty-queue. v2.20 unpublished-delete is already on `main` `ba3c85cec8e100e289e25e6a33fbf9440676c26e` and is NOT a fifth wave. `PROTOCOL.md` is every-guideline law (v2.20); Continentie evidence sentences in v2.16–v2.19 MUST remain. Wave A (next implementation only): context-aware splitter + testable reject function; an inhoudelijk knowledge object MUST be one complete, independently readable meaning unit with bronpassage+locator; MUST NOT be only a number, label, kopwoord, nav, stamp, or sentence fragment; MUST NOT duplicate identical `clean_text` from the same freeze; MUST NOT treat “Inleiding” as chrome; Home/Tools/Richtlijnen/Meedenken are chrome; Continentie regression fixtures; no infrastructure in wave A. Wave B: G2 status MUST NOT depend on a stale static JSON field; publication gate MUST NOT open because an app-setting is present; do not claim G2 PASS. Wave C: finish PR #82, do not activate until a test App Service exists; four known #82 faults; separate deployment identities. Wave D: backup/recoverability of `/home/data/metis-console`; no large database migration. G2-readiness (PR #69) already pinned azure-identity/azure-storage-blob; G2 remains BLOCKED; `publish()` remains G2-BLOCKED. Protocol-only; does not implement console, extract or Azure. Next implementation is wave A only, then B, then C, then D. Not Azure ZIP of v2.20 until A is on a controlled SHA unless the owner re-opens Cloud Shell. v2.14 is not this and is not next. G2 remains the publication blocker.
- Protocol v2.20 implementation on the existing kernel/console: real delete control on the document card and Review chooser for unpublished snapshots only (researcher Dutch **Verwijder unpublished document**; confirm required; no-confirm does not delete). After delete that snapshot is gone from Inleveren, Review and Documentenhiërarchie; stored objects+envelope for that `snapshot_id` are gone; freeze bytes of that unpublished source MAY be removed with it. Audit ledger row records who, when, `snapshot_id`, source SHA-256 and title. Capture is not publication. MUST NOT delete a published projection; MUST NOT hide selected objects inside a freeze that stays in Review; MUST NOT touch other snapshots or `/home/data` globally; MUST NOT SSH/wipe `/home/data` as the product path. Four-eyes is not required; the uploader MAY delete unpublished they captured; a second named reviewer is not required for delete. Operator is the logged-in researcher/reviewer, not Metis / Implementation engineer / Auditor. Serving/G2 unchanged; `publish()` remains G2-BLOCKED. v2.16 tiny-objects, v2.17 chrome/slogans/bronpassage-prose, v2.18 no-duplicate-sentence and v2.19 Koppen batch / Inhoud duty queue still hold. Freeze store paths remain confined (allowlist filename/snapshot/date/version before any filesystem join). Protocol PR #80 remains separate. Beoordeel timeout / performance is out of scope. Azure/G2, v2.14, LLM and `publish()` PASS stay out of scope.
- Protocol v2.20.0: `PROTOCOL.md` is the law of V&VN Data Services for every guideline, not Continentie-only. Continentie in v2.16–v2.19 remains live evidence of fails (stamp words as Koppen; 2008 Inhoud cards), not the product identity; those historical evidence sentences MUST remain and MUST NOT be stripped; the next freeze MUST NOT have to be Continentie. Unpublished captured snapshots MAY be removed from the operations console by an authorized console operator (same class as ingest: named researcher/reviewer account, not a secret engineer path). This is owner-authorized cleanup of unpublished capture, not publication, not G2, not hiding fragments of a freeze that remains in Review. MUST a real delete control on the document card / Review chooser for unpublished snapshots only; researcher-Dutch label (for example **Verwijder unpublished document**); MUST confirm before it runs (destructive). After delete that snapshot MUST NOT appear on Inleveren/Review/Documentenhierarchie lists; stored objects+envelope for that `snapshot_id` are gone; freeze bytes of that unpublished source MAY be removed with it; MUST NOT touch other snapshots or `/home/data` globally; MUST append an audit ledger row (who, when, `snapshot_id`, source SHA-256, title). Capture remains not publication. MUST NOT delete a published projection or anything that has been published; `publish()` remains G2-BLOCKED; there is no published Continentie. MUST NOT hide selected objects inside a freeze that stays in Review (v2.16 hide-fragments-without-extract remains); whole unpublished snapshot only. MUST NOT SSH/wipe `/home/data` as the product path; the console action is the path. Four-eyes is not required to delete unpublished capture; the uploader MAY delete unpublished they captured; a second named reviewer is not required for delete (delete is not type-confirm). Serving/G2/four-eyes/v2.14/Azure unchanged; four-eyes unchanged for type-confirm and high-risk; `publish()` remains G2-BLOCKED. Protocol-only; does not implement console, extract or Azure. Next implementation is this unpublished-delete wave on the existing kernel, then William MAY remove live unpublished Continentie and ingest another HTML freeze, then William click-through, then Azure ZIP from a V&VN-trusted device after William accepts the live console, then G2. Beoordeel timeout / performance is a separate issue and is not this delta. v2.14 is not this and is not next. G2 remains the publication blocker.
- Protocol v2.19 implementation on the existing kernel/console: researchers are not required to open thousands of Inhoud cards one by one (Koppen 78 / Inhoud 2008 style fail). Koppen stay batch-confirmable as structure, never as advice. The presented slow duty is proposed `recommendation` plus `condition` / `exception` / any high-risk object (four-eyes unchanged). Remaining `unclassified` stays in the store and is acknowledged by count, not as equal one-by-one cards; unclassified is never served. No `zwaar/licht` switch; MUST NOT auto-confirm types; MUST NOT auto-promote ordinary text to `recommendation`; the machine MUST NOT decide something is light enough to serve. Only confirmed `recommendation` MAY be `supported` / handelingsadvies. v2.16 tiny-objects, v2.17 chrome/slogans/bronpassage-prose and v2.18 no-duplicate-sentence still hold. Unpublished Continentie MAY be re-extracted on the same SHA-256; unpublished identities MAY be replaced; MUST NOT hide stored fragments without that extract. Serving/G2 unchanged; four-eyes unchanged; `publish()` remains G2-BLOCKED. Capture is not publication. Protocol PR #78 remains separate.
- Protocol v2.19.0: researchers MUST NOT be required to open 2008 Inhoud cards one by one (same fail as 4000 unclassified: fatigue, not assurance). Live evidence: Inhoud (2008) / Koppen 78 on Continentie after the v2.18 extract (`main` `4ebfdbb88cdb`, snapshot `snap-ac59cf24f946088e-e402c4d3`). Owner said it looks better, then asked whether researchers must do the 2008 objects by hand, then “zet dat in protocol.” Koppen MAY and MUST be batch-confirmable as structure, never as advice. The researcher-required slow review is proposed `recommendation` plus `condition` / `exception` / any high-risk object (four-eyes unchanged). Remaining `unclassified` MUST NOT be presented as equal one-by-one work of thousands of cards. Unclassified is never served, so 2000 clicks on it do not add assurance. Serving stays fail-closed: only confirmed `recommendation` MAY be `supported` / handelingsadvies. MUST NOT auto-confirm types. MUST NOT auto-promote ordinary text to recommendation. MUST NOT a researcher “zwaar/licht” or “snel/langzaam” switch. Definition/explanation are NOT the MVP 2000-card duty for handelingsadvies. Extract SHOULD still get coarser but this delta’s bar is review DUTY and queue presentation, not a new object type. 2008 unclassified/Inhoud cards remains a fail. Unpublished Continentie MAY be re-extracted; source SHA-256 stays; unpublished identities MAY be replaced; MUST NOT hide stored fragments without a new extract. Serving/G2/four-eyes/v2.14/Azure unchanged; four-eyes unchanged; `publish()` remains G2-BLOCKED. Protocol-only; does not implement console, extract or Azure. Next implementation is this queue/duty wave on the existing kernel, then William click-through, then Azure ZIP from a V&VN-trusted device after William accepts the live Review page, then G2. v2.14 is not this and is not next. G2 remains the publication blocker.
- Protocol v2.18 implementation on the existing kernel/console: the open review object card shows the freeze sentence once (MUST NOT duplicate it as both h3/title and body; compact row/card: one source sentence plus short status). Extract MUST NOT split a grammatical continuation / trailing clause into a new object (`Eventueel met hulp van de mantelzorger.`, `Bijvoorbeeld …` stay with the sentence they complete). Extract MUST NOT emit a second knowledge object whose visible freeze prose (`clean_text`) is identical to one already emitted from this freeze; distinct real headings MAY remain (`1.1 Inleiding` vs `2. Inleiding`). Unpublished Continentie MAY be re-extracted on the same SHA-256; unpublished identities MAY be replaced; MUST NOT hide stored fragments without that extract. v2.17 chrome/slogan/bronpassage-prose held. Serving/G2 unchanged; four-eyes unchanged; `publish()` remains G2-BLOCKED. Capture is not publication. Protocol PR #76 remains separate.
- Protocol v2.18.0: the open review object card MUST show the freeze sentence once (MUST NOT duplicate it as both h3/title and body). Compact row / card: one source sentence plus short status; the v2.16 compact-row bar applies to the open card, not only the list. Extract MUST NOT split a grammatical continuation / trailing clause into a new object (restates and tightens the v2.16 truncated-sentence / tiny-object forbid; the v2.17 Continentie re-extract still failed it). Extract MUST NOT emit a second knowledge object whose visible freeze prose (`clean_text`) is identical to an object already emitted from this freeze; repeated HTML (samenvatting versus module) is not extra knowledge; distinct real headings in different sections MAY remain (`1.1 Inleiding` vs `2. Inleiding`). MUST NOT hide stored fragments without a new extract; unpublished Continentie MAY be re-extracted; source SHA-256 stays; unpublished identities MAY be replaced. Chrome Tools/Home gone (v2.17 held). Slogan gone (v2.17 held). This fail is duplication + truncated-sentence split. Serving/G2/four-eyes/v2.14/Azure unchanged; four-eyes unchanged; `publish()` remains G2-BLOCKED. Protocol-only; does not implement console, extract or Azure. Next implementation is this extract+card wave on the existing kernel, then William click-through, then Azure ZIP from a V&VN-trusted device after William accepts the live Review page, then G2. v2.14 is not this and is not next. G2 remains the publication blocker.
- Protocol v2.17 implementation on the existing kernel/console: researcher copy without slogans (lead copy says Beoordeel Koppen as structure and Inhoud as knowledge objects; the entire sentence “Dit wordt wat een EPD MAG zeggen.” is gone); HELP_ONCE via-negativa removed from researcher rooms; Onderwerp/family empty on a fresh ingest (not prefilled `continentie`); bronpassage is readable prose derived from the v2.11 locator on every object (tags/CSS classes stripped for display only; freeze bytes stay exact); extract MUST NOT emit kennisplatform chrome (Tools, Home, Richtlijnen, Meedenken and similar site nav) as objects or Koppen; recommendation-strength UI only on type `recommendation`; relation checkbox label sits next to the checkbox; unpublished Continentie MAY be re-extracted on the same SHA-256; MUST NOT hide stored fragments without a new extract. Serving/G2 unchanged; headings MUST NOT be handelingsadvies; four-eyes unchanged; `publish()` remains G2-BLOCKED. Capture is not publication. Protocol PR #74 remains separate.
- Protocol v2.17.0: researcher UI copy MUST be researcher language, no slogans, no “wat een EPD MAG zeggen”, no single-subscriber claim. The entire sentence “Dit wordt wat een EPD MAG zeggen.” MAG weg; do not weaken. Review lead copy says what to do (Beoordeel Koppen as structure, Inhoud as knowledge objects). Via-negativa MUST NOT appear on researcher rooms, including collapsed HELP_ONCE “Over deze console”. Onderwerp/family MUST be empty on a fresh new ingest (not prefilled `continentie`; class default out of scope). Bronpassage MUST show the same readable sentence as the knowledge object, without HTML tags, CSS class names or kennisplatform markup, on every object / the whole freeze (recorded answer to “Is dat hoe het definitief eruit gaat zien?” is NO); v2.11 freeze bytes and locators stay exact (MUST NOT reserialize). Extract MUST NOT emit kennisplatform chrome as knowledge objects or Koppen, including one-word Tools/Home/Richtlijnen/Meedenken. Recommendation-strength UI MUST NOT appear except on type `recommendation`; a nav word MUST NOT get a stamp picker. Relation checkbox and its label MUST be adjacent (MUST NOT stretch across the viewport). These rules apply to the whole freeze / every object / every section; Inleiding, Doel, Doelgroep and Aanleiding are examples, not a closed list. 2641 unclassified on one richtlijn remains a fail. Unpublished Continentie MAY be re-extracted; source SHA-256 stays; unpublished object identities MAY be replaced; MUST NOT hide stored fragments without a new extract. Serving/G2 unchanged; four-eyes unchanged; `publish()` remains G2-BLOCKED. Protocol-only; does not implement console, extract or Azure. Next implementation is that researcher-surface wave on the existing kernel, then William click-through, then Azure ZIP from a V&VN-trusted device after William accepts the live Review page, then G2. v2.14 is not this and is not next. G2 remains the publication blocker.
- Protocol v2.16 implementation on the existing kernel/console: one door **Beoordeel** (Openen+Reviewen dual doors gone); two named stacks with counts (Koppen / Inhoud); compact one-line source-text rows plus short status (not type name, not kernel id, not three-column); DOEN/OVERWEEG/NIET DOEN are stamps on `recommendation` (`doen` | `overweeg` | `niet_doen`) with Dutch researcher sentence, not objects and not Koppen rows; extract MUST NOT heading-propose those words even if tagged as heading; extract MUST NOT emit list-number-only, stamp-only or truncated-sentence objects; new extract of unpublished Continentie keeps freeze SHA-256 and MAY replace unpublished object identities; UI MUST NOT hide stored fragments without that extract. Serving/G2 unchanged: only confirmed `recommendation` MAG `supported`; headings MUST NOT be handelingsadvies; four-eyes unchanged; `publish()` remains G2-BLOCKED. Capture is not publication. Protocol PR #72 remains separate.
- Protocol v2.16.0: the Review page MUST convince guideline researchers (if it does not suffice, the project has failed even if the kernel is fail-closed). Two frustrations MUST NOT remain (unclear: two doors, rows split across the page, DOEN/OVERWEEG without explanation; too-small objects: one word, one number, half a sentence). One door **Beoordeel**. Two named stacks with counts (Koppen / Inhoud). Compact one-line source-text rows. DOEN/OVERWEEG/NIET DOEN are stamps on `recommendation` (`doen` | `overweeg` | `niet_doen`), not objects and not Koppen rows. Extract MUST NOT heading-propose those words and MUST NOT emit tiny objects. Continentie is unpublished: a new extract of the same freeze bytes is REQUIRED; source hash stays; unpublished object identities MAY be replaced. MUST NOT lie in the UI by hiding stored fragments without a new extract. Serving/G2 unchanged; four-eyes unchanged. Protocol-only; does not implement console, extract or Azure. Next implementation is that review-page wave on the existing kernel, then William click-through, then Azure ZIP from a V&VN-trusted device, then G2. v2.14 is not this and is not next. G2 remains the publication blocker.
- Protocol v2.15 implementation on the existing kernel/console: ingest page 1 requires a calendar source date (NL `dd-mm-jjjj` on screen, ISO `YYYY-MM-DD` stored; empty rejected; not today) and a dotted-integer source version; extract proposes `heading` for real source headings/TOC crumbs without rewriting the v2.13 split or re-extracting already-hashed Continentie objects; review list titles are freeze source-passage snippets (not the type name); review queues route by type (fast heading batch-confirm as structure, slow one-object card) without a zwaar/licht switch. Four-eyes and fail-closed serving unchanged. `publish()` remains G2-BLOCKED. Capture is not publication. Protocol PR #70 remains separate.
- Protocol v2.15.0: ingest source date is a Europe/Amsterdam calendar picker stored as ISO `YYYY-MM-DD` (required; colofon/publicatiedatum; not today; not ingest-click). Ingest source version is required dotted non-negative integers (`^[0-9]+(\.[0-9]+)*$`); no `v` prefix, letters, year-as-version. Extract MUST propose `heading` for real source headings so they do not all land as `unclassified`. Review list MUST show a source-passage snippet and MUST NOT use the type name as the row title. Fast lane MUST batch-confirm proposed headings as structure; slow lane stays one-object. Four thousand unclassified cards on one richtlijn is a fail of this review surface. Does not rewrite v2.13 split rules; MUST NOT re-extract existing Continentie bytes. Protocol-only; does not implement console or kernel. Next implementation is that console wave on the existing kernel, then G2/Azure. v2.14 is not this and is not next. G2 remains the publication blocker.
- G2-readiness (not G2 PASS): pin Azure Blob SDK in pyproject runtime deps (`azure-identity==1.25.3`, `azure-storage-blob==12.30.1`); report-only Azure preflight (container, `vvn-metis-console` identity, Storage Blob Data Contributor on `aidataservice/canonical-sources` only, SDK, activation setting); activation runbook; inert synthetic SHA-256 smoke (does not run). Adapter already exists. Container `canonical-sources` remains empty. G2 remains BLOCKED; `publish()` remains fail-closed; G0 Azure DEV remains BLOCKED. No live Azure mutation.
- G2 Azure Blob ingest adapter: console ingest stores exact source bytes content-addressed in the private `canonical-sources` container, reads them back and verifies SHA-256 before creating a snapshot. The `azure://` locator is bound to the envelope, and a missing or corrupt local cache is restored from the verified blob. Authentication uses managed identity/Entra ID only. `publish()` remains fail-closed.
- Review room loads one object card per request (`/review?document=&object=`). Document view lists objects without opening every freeze passage. Fixes Azure App Service 504 Gateway Timeout on large Continentie reviews. Two-column card unchanged. `publish()` remains G2-BLOCKED.
- Azure App Service host adapter for the internal operations console (`src/console_asgi.py`). Bootstrap accounts from environment only; G2 remains BLOCKED; G0 Azure DEV remains BLOCKED until the web app is live. Not a public website.
- Owner lock 2026-08-31: G2 Blob store recorded in code (`aidataservice` / `canonical-sources`, West Europe, private). Locator `azure://aidataservice/canonical-sources/{sha256}/{filename}`. Container empty; G2 remains BLOCKED; `publish()` remains G2-BLOCKED; G0 Azure DEV remains BLOCKED. No storage keys in Git.
- Review card two-column layout (owner lock 2026-08-29): knowledge object left (type, relation checkboxes, decision), exact freeze bronpassage right (v2.11 locators via `open_source_passage`); stacks on narrow. Type/approve stay blocked if the passage cannot open. Relations stay proposed checkboxes + confirm. No graph editor. Implements the already-approved docs lock; not a new protocol version. `publish()` remains G2-BLOCKED. v2.14 is not next.
- Owner lock 2026-08-29: reviewer bronpassage is two columns on the review card (object left, freeze passage right; stack on narrow). Relations stay proposed checkboxes + confirm; MUST NOT build a graph editor. Next console implementation after this docs lock is the split-screen review card only. Then G2/Azure. `publish()` remains G2-BLOCKED. Docs-only; does not implement the card. v2.14 is not next.
- Console review completeness on the existing v2.13 kernel: relation checkboxes POST to `confirm_relations()`; type select defaults to disabled «nog niet bevestigd» and does not silently submit heading; type/approve require open-original; `published_object_type()` serves only a confirmed closed type; GET `/v1/knowledge/{id}` 404s/abstains without locator or with unclassified/historical type. `publish()` remains G2-BLOCKED.
- Protocol v2.13.0: one knowledge object is one confirmable meaning unit; extraction splits at meaning boundaries, not token budgets; fusion of condition into recommendation is the forbidden default; per-type classification rules on the unchanged v2.12 closed serving typeset; closed relations (applies_if, except_if, defines, explains, supported_by, supersedes, parent/child); high-risk four-eyes on the v2.12 tuple; reviewer MUST open the exact source passage (v2.11 locators). Protocol-only; does not implement extract, relations, open-original, schema or publish(). Next implementation is that kernel follow-up, then G2/Azure. v2.14 is not next. G2 remains the publication blocker.
- Protocol v2.13 kernel on the existing kernel: meaning-boundary split (token budget does not define object identity; fusion of condition/exception into recommendation rejected except one grammatical claim); schema v1.2 serving-law relation names with proposed vs confirmed relations; unconfirmed relations do not bind; historical types are not served; four-eyes on the v2.12 tuple (exception, listed risk fields; agents excluded; uploader insufficient); open-original from freeze bytes + v2.11 locators in the review room; Product API serves a published recommendation together with published applies_if/except_if targets. `publish()` remains G2-BLOCKED. Capture is not publication. v2.14 is not next.
- Implementation wave A+B+C on the existing kernel (one PR against main): v2.12 unclassified default, question×type answerability, object-tuple review binding and atomic published projection; v2.11 ingest lock in code (live URL-HTML rejected; freeze HTML and hashed PDF URL accepted; `supported` requires source_locator); v2.10 Documentenhierarchie heading, waiting-task badges and Accounts room with closed roles. `publish()` remains G2-BLOCKED. No Azure/Vercel/Neon/LLM.
- Protocol v2.12.0: extraction is structure/provenance only (unclassified default; human confirms object_type); answerability is question type × object type; publish binds object_id + object_version + canonical_object_hash + confirmed_object_type + reviewer + decision; serving uses an atomically replaced published projection. Protocol-only; does not implement extract, API or console changes. Next implementation is that kernel follow-up, then G2/Azure. G2 remains the publication blocker.
- Protocol v2.11.0: uploaded HTML freeze file, reject live URL-HTML at ingest, mandatory source locators on knowledge objects, and fail-closed Product API `supported` without a locator. Now a live component of baseline v2.12.0 plus this v2.11 delta. Protocol-only; does not implement ingest rejection or API fail-closed. v2.11 kernel work remains required law and is not a supersession of v2.12 sequencing. G2 remains the publication blocker. Does not reopen GD-03.
- Protocol v2.10.0: Documentenhierarchie heading, real waiting-task badges, and Accounts room with closed role set (researcher, reviewer, publisher). Protocol-only; does not implement the UI follow-up. Next implementation is that follow-up on the now-merged v2.9 UX (PR #25). G2 remains the publication blocker.
- Console-UX-rewrite on the existing kernel: task-oriented researcher rooms, researcher vocabulary (no envelope as a UI term), visible move/promote actions, login with gebruikersnaam and `type=password`, V&VN digital stylesheet tokens (Water secondary family, system-font fallback) and the official v&vn beeldmerk PNG. Protocol v2.9.0 remains the live UX/brand law; this implements that rewrite in `src/operations_console_app.py` (PR #25). G2 remains the publication blocker.
- Protocol v2.9.0: researcher-task console UX and V&VN digital stylesheet (huisstyle) rules. Protocol-only; does not implement the UI rewrite. Next implementation is that rewrite on the existing kernel. G2 remains the publication blocker.
- Internal operations console MVP: ingest mailbox (HTML/PDF or URL snapshot), family × class tree, named reviewers, review return-loop, local G0 identity. Served by `vvn-data-service serve-console`. Publication remains fail-closed without an immutable locator (G2).

### Changed
- Reviewpagina herordend rond kennisobject → classificatie → besluit: begrijpelijke Nederlandse type- en statuslabels, leesbare relatiebevestiging, brononderbouwing naast de beoordeling en een expliciete, aanvankelijk lege besluitkeuze. Toelichting is verplicht bij revisie of afwijzing; de primaire knop en zichtbare velden volgen het gekozen besluit. Kernel-, rol-, four-eyes- en publicatielogica zijn ongewijzigd.
- Console workspace: Water-light remains only the outer edge. Interior rooms sit on a wide white canvas instead of an A4-width card. The in-page Metis mark is the geometric lockup on white (no black field), with a two-row header and more space under the logo before room copy. Room pages keep one short task line; via-negativa stays in help (Protocol v2.9). Researcher-facing axis label is onderwerp (kernel field remains family). Publish still states that publication remains geblokkeerd without durable storage. `publish()` remains G2-BLOCKED.
- Removed `HANDOFF.md` as a steering layer. Current progress is read from merged code, tests, CI and commit history on `main`; protocol and roadmap retain norms, sequence and stop conditions without duplicating implementation status.
- Historical step, audit and repair reports moved from the repository root to `docs/history/`. The root remains the operating surface; `output/` historical artefacts are unchanged.

### Fixed
- Azure console packaging pins `cryptography==44.0.3`, whose manylinux wheel is compatible with the App Service Python 3.12 image; this prevents a vendored newer wheel from failing at startup on an unavailable `GLIBC_2.33` symbol.
- Console freeze store paths reject `..` and separators in user-provided filename, snapshot id, and ingest date/version before any filesystem join, so objects and source bytes cannot leave their store directories.
- Azure console accounts, review state and uploaded source freezes now live under persistent `/home/data/metis-console`, outside deployment-managed `wwwroot`. The Azure startup path has the same fail-safe default, so a ZIP deployment no longer resets operator data when `CONSOLE_DATA_ROOT` is omitted.
- Restored V&VN Data Services handoff/roadmap after foreign product status was written into this repository.
- Canonical store now uses the integrity-kernel object hash and exact review snapshot (closes remaining P0 dual-hash).
- CLI `serve` / `serve-api` match the Docker entrypoints.
- Inspection search uses the same answerability gate as the Product API.
- `audit-current` defaults to reviewed fixtures instead of `output/`.

## [0.1.0] - 2026-08-20

### Added
- Canonical knowledge-object pipeline with provenance and immutable hashing.
- Clinical review and four-eyes workflow for high-risk objects.
- Publication registry and fail-closed publication gates.
- Lexical, vector and hybrid retrieval.
- Protocol v2.1 Answerability/Evidence Gate and safe abstention behavior.
- External Product API v1 with tenant scopes, entitlements and usage metering.
- Generic HTML source adapter and source-neutral provenance schema v1.2.
- Repository baseline with CI, repository preflight and contribution rules.

### Known blockers
- Canonical source binary for Fractuurpreventie is not yet locally verified.
- Second real source (Continentie) awaits exact source bytes before canonical ingest.
- Independent retrieval acceptance requires a new holdout B.
- RAG/Answer API acceptance has not started.
